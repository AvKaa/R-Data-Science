# assigning values to variables
a <- 1
b <- 1
c <- -1

# solving the quadratic equation
sol1 <- (-b + sqrt(b^2 - 4*a*c))/(2*a)
sol2 <- (-b - sqrt(b^2 - 4*a*c))/(2*a)

# list defined objects
ls()

# to run the whole script press ctrl + shift + enter


# Functions
exp(a) +log(a)
exp(log(a))

# functions are evaluated inside out
# to look for what the function is for type ? infront of function
# args(function) function shows the arguments that the function needs:

args(log)

log(8,base = 2)
log(x = 8,base = 2)
x


